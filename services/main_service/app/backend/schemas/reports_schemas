from pydantic import BaseModel
from datetime import datetime
from app.backend.databases.models import ReportStatus

class ReportCreate(BaseModel):
    report_type: str

class ReportResponse(BaseModel):
    id: int
    user_id: int
    status: ReportStatus
    created_at: datetime
